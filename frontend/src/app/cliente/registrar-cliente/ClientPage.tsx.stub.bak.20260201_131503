"use client";

import dynamicImport from "next/dynamic";


export const dynamic = "force-dynamic";

const RegistrarClienteClient = dynamicImport(() => import("./ClientPageBridge"), {
  ssr: false,
});

export default function Page() {
  return <RegistrarClienteClient />;
}
